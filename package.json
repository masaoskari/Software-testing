{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "npm run mocha && npm run jest",
    "mocha": "mocha tests/test_mocha.js",
    "jest": "npx jest"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-junit", { "outputDirectory": "./test-results/junit" }]
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.10",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "mocha": "^10.2.0"
  }
}